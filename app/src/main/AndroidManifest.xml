<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cl.ubb.fastfood">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".detallessandwitch"
            android:label="Detalles Sandwich"
            android:parentActivityName=".sandwitch">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".sandwitch" />

        </activity>

        <activity android:name=".visordeimagen"
            android:label="Sandwich"
            android:parentActivityName=".sandwitch">
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value=".sandwitch" />
        </activity>


        <activity
            android:name=".sandwitch"
            android:label="Nuestros Sandwich"
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity
            android:name=".nosotros"
            android:label="Sobre Nosotros"
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>